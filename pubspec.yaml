name: appfuse
description: orchestrator for Flutter apps

homepage: https://github.com/ulughbeck/appfuse
repository: https://github.com/ulughbeck/appfuse
issue_tracker: https://github.com/ulughbeck/appfuse/issues

topics:
  - scope
  - settings
  - state-management
  - dependency-injection
  - appfuse

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

version: 0.0.2

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  shared_preferences: ^2.5.5
  package_info_plus: ^10.1.0
  platform_info: ^5.0.0
  permission_handler: ^12.0.3
  http: ^1.6.0

dev_dependencies: 
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
